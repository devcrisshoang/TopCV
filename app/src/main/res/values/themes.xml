<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TopCV" parent="Theme.Material3.DayNight.NoActionBar">

    </style>

    <style name="Theme.TopCV" parent="Base.Theme.TopCV" />

    <style name="Theme.TopCV.Fullscreen" parent="Theme.TopCV">
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.TopCV.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>